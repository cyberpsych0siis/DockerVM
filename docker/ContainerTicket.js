export default function ContainerTicket(container) {
  /*return {
      id: newUuid,
      reachableHostname: reachableHostname,
      socket: socketAddress,
      channels: channels,
    };*/

  console.log(container);

  return container;
}
