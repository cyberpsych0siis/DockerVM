module.exports = class LabelProvider {
/*     constructor() {

    } */

    /**
     * Should return options on how the element is connected to the proxy
     */
    getProperties(containerName,
            reachableAddress,
            proxyPort) {
        return {}
    }
}